// Re-export date utilities from dateUtils (Mock data removed for live API integration)
export { 
  formatDateKey, 
  getTodayKey, 
  getPastDays, 
  calculateStreak, 
  CATEGORIES, 
  getCategoryMeta 
} from '../utils/dateUtils';
